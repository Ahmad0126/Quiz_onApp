import 'package:flutter/material.dart';

class Constants{
  PreferredSizeWidget appBar = AppBar(
    centerTitle: true,
    title: const Text('Quiz Online'),
  );
  Color background = Colors.grey.shade900;
}